import React from "react"

/*
export default function Pagination({currentPage, nextPage, numPages}) {
  return (
    <p>
      Page: {currentPage} of {numPages} {
         currentPage !== numPages? <Link to={`/blog/${nextPage}`}>Older</Link>: false}
    </p>
  )
}
*/
